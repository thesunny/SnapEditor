# The following don't require CoffeeScript files because they use AMD.
define ["jquery.ui.widget", "load-image", "canvas-to-blob", "jquery.iframe-transport", "jquery.fileupload", "jquery.fileupload-image"], ->
